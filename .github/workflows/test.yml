---
name: 🧪 Test

on:
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        id: harden_runner
        uses: step-security/harden-runner@ec9f2d5744a09debf3a187a3f4f675c53b671911 # v2.13.0
        with:
          egress-policy: audit

      - name: Checkout
        id: checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Build Image
        id: build_image
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 # v6.18.0
        with:
          file: Dockerfile
          push: false
          load: true
          tags: ghcr.io/ministryofjustice/analytical-platform-ui:${{ github.sha }}

      - name: Run Python Tests
        id: run_python_tests
        run: |
          docker compose --file contrib/docker-compose-test.yml run --rm interfaces
        env:
          NETWORK: default
          CONTAINER_IMAGE_NAME: ghcr.io/ministryofjustice/analytical-platform-ui
          CONTAINER_IMAGE_TAG: ${{ github.sha }}
