{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "aws-sso-process",
      "type": "shell",
      "command": "aws-sso login && aws-sso process --sts-refresh --profile analytical-platform-next-poc-hub-development:platform-engineer-admin"
    }
  ]
}
