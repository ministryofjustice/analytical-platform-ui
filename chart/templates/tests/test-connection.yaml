apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "analytical-platform-ui.fullname" . }}-test-connection"
  labels:
    {{- include "analytical-platform-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "analytical-platform-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
